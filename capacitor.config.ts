import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.skillcraftnextgen.app',
  appName: 'SkillCraft NextGen',
  webDir: 'dist'
};

export default config;
